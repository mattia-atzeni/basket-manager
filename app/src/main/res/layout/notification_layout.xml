<?xml version="1.0" encoding="utf-8"?>

<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:layout_gravity="center"
    android:clickable="true"
    style="@android:style/Widget.ActionButton">

    <ImageView
        android:id="@+id/basketball_icon"
        android:src="@mipmap/ic_notification"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:layout_margin="0dp"
        android:contentDescription="basketball_icon"
        />

    <TextView xmlns:android="http://schemas.android.com/apk/res/android"
        android:id="@+id/notifications_count"
        android:layout_width="wrap_content"
        android:minWidth="23sp"
        android:minHeight="23sp"
        android:textSize="14sp"
        android:text="0"
        android:textColor="#ffffffff"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:layout_alignTop="@id/basketball_icon"
        android:layout_alignRight="@id/basketball_icon"
        android:layout_marginRight="0dp"
        android:layout_marginTop="3dp"
        android:paddingBottom="1dp"
        android:paddingRight="4dp"
        android:paddingLeft="4dp"
        android:background="@drawable/notification_background"/>
</RelativeLayout>